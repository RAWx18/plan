Now I'll create the comprehensive Decision Point Audit for the Caracal authority system:

## CARACAL DECISION POINT AUDIT
### Complete Security & Correctness Analysis

---

| **#** | **DECISION POINT** | **WHO/WHAT MAKES IT (file:line)** | **WHAT inputs depend on** | **WHAT CAN BYPASS IT** | **WHAT FAILS IF WRONG** |
|---|---|---|---|---|---|
| **1.1** | **Principal Registration / validate name uniqueness** | [identity.py:174](packages/caracal-server/caracal/core/identity.py#L174) `PrincipalRegistry.register_principal()` checks `query(Principal).filter_by(name=name).first()` | `name` (must be unique), `principal_kind` (one of: HUMAN, ORCHESTRATOR, WORKER, SERVICE) | **NONE DETECTED** - uniqueness enforced at query level | Duplicate principal names can exist → confusing identity resolution, potential impersonation |
| **1.2** | **Principal Lifecycle Entry validation** | [identity.py:165-167](packages/caracal-server/caracal/core/identity.py#L165-L167) `register_principal()` sets ORCHESTRATOR/WORKER to PENDING_ATTESTATION if not ATTESTED | `principal_kind`, `attestation_status` (defaults to UNATTESTED) | Can be bypassed by passing `attestation_status="attested"` at registration → **non-attested agents activate directly** | Attestation requirement gated → unattest agents can act |
| **1.3** | **Lifecycle State Machine Enforcement** | [identity.py:219-223](packages/caracal-server/caracal/core/identity.py#L219-L223) `transition_lifecycle_status()` calls `lifecycle_state_machine.assert_transition_allowed()` | `from_status`, `to_status`, `attestation_status`, `principal_kind` | Attestation status in metadata can be bypassed if `assert_transition_allowed()` logic missing → **FAIL-CLOSED check** but depends on implementation | Wrong state transitions allow revoked principals to reactivate, SERVICE principals to become active |
| **2.1** | **Mandate Issuance / Policy Enforcement** | [mandate.py:256-400](packages/caracal-server/caracal/core/mandate.py#L256-L400) `MandateManager.issue_mandate()` validates `enforce_issuer_policy=True` (default) | `issuer_id` (must have active AuthorityPolicy), `validity_seconds` (must ≤ policy.max_validity_seconds), `resource_scope` + `action_scope` (must be subset of policy) | **CRITICAL BYPASS**: `enforce_issuer_policy=False` parameter **allows mandate issuance with NO policy checks** (line 296) → policy can be completely skipped | Unvalidated mandates issued beyond issuer authority bounds → scope creep, policy violations |
| **2.2** | **Subject Principal Validation** | [mandate.py:378-388](packages/caracal-server/caracal/core/mandate.py#L378-L388) checks `subject_principal.lifecycle_status != ACTIVE` → raises ValueError | `subject_id`, `lifecycle_status` of subject | **NONE DETECTED** - properly enforced | Dead/revoked subjects receive valid mandates → authority to unauthorized principals |
| **2.3** | **Mandate Signature Generation** | [mandate.py:398-411](packages/caracal-server/caracal/core/mandate.py#L398-L411) calls `SigningService.sign_canonical_payload_for_principal()` with issuer private key | `issuer_id` (private key must exist in vault), `mandate_data` (canonical JSON) | Vault key resolution failure silently caught at line 409-412 (`except SigningServiceKeyError`) → **mandate never created** (fail-closed) | Unsigned mandate stored without signature check → forged mandates could be created directly in DB |
| **2.4** | **Rate Limiting Check** | [mandate.py:317-327](packages/caracal-server/caracal/core/mandate.py#L317-L327) `if self.rate_limiter:` checks rate limit, else skips | `issuer_id`, `rate_limiter` (optional, can be None) | **Rate limiter is optional** (`if self.rate_limiter:`) → **no rate limit if not configured** (line 318-320) | Issuer can spam mandate creation → DoS, quota exhaustion |
| **3.1** | **Authority Validation / Mandate Existence** | [authority.py:541+](packages/caracal-server/caracal/core/authority.py#L541) `validate_mandate()` first checks mandate is not None, then runs 6 boundary stages | `mandate_id` (must exist in DB or cache), `requested_action`, `requested_resource` | Mandate not found returns DENY (fail-closed at line ~560) | N/A (fail-closed) |
| **3.2** | **Issuer Authority Checks (Stage 1)** | [authority.py:559-588](packages/caracal-server/caracal/core/authority.py#L559-L588) checks issuer exists and is ACTIVE with valid public key | `issuer_id` (from mandate), `issuer.lifecycle_status` | **LOOSE CHECK**: only validates issuer exists + is ACTIVE, no attestation requirement for issuers | Suspended issuers can still issue valid mandates (only revoked/dead blocks them) |
| **3.3** | **Mandate State Validation (Stage 2)** | [authority.py:658-700](packages/caracal-server/caracal/core/authority.py#L658-L700) checks revoked, valid_from ≤ now ≤ valid_until | `revoked` flag, `valid_from`, `valid_until`, `revocation_reason` (if revoked) | **NONE DETECTED** - tight time/revocation checks | Expired/not-yet-valid mandates used → time-of-check-time-of-use (TOCTOU) could be exploited if clock is unreliable |
| **3.4** | **Delegation Path Validation (Stage 3)** | [authority.py:703-733](packages/caracal-server/caracal/core/authority.py#L703-L733) calls `DelegationGraph.validate_path()` if `delegation_graph` is set | `mandate.source_mandate_id`, `delegation_graph` (optional, can be None) | **Delegation validation is optional**: `if self.delegation_graph:` (line 705) → **no path validation if graph not configured** | Invalid delegation chains allowed → authority escalation through broken chain paths |
| **3.5** | **Subject Binding Validation (Stage 4)** | [authority.py:736-758](packages/caracal-server/caracal/core/authority.py#L736-L758) checks `mandate.subject_id == caller_principal_id` | `mandate.subject_id`, `caller_principal_id` (from request) | **CRITICAL**: Missing subject binding check if mandate has no subject binding requirement in design → defaults to mandate subject_id match | Wrong principal can use another's mandate → authority assumption |
| **3.6** | **Caveat Chain Validation (Stage 5)** | [authority.py:761-787](packages/caracal-server/caracal/core/authority.py#L761-L787) calls `verify_caveat_chain()` + `evaluate_caveat_chain()` if chain provided | `caveat_chain`, `caveat_hmac_key`, `requested_action`, `requested_resource`, `task_id` (optional, per caveat type) | **Caveat validation is optional**: chain can be empty/None → skips all caveat narrowing | Caveat-restricted mandates used without caveat enforcement → scope narrowing bypassed |
| **3.7** | **Action/Resource Scope Authorization (Stage 6)** | [authority.py:790-829](packages/caracal-server/caracal/core/authority.py#L790-L829) matches `requested_action` against mandate `action_scope` patterns (wildcard) + `requested_resource` against `resource_scope` | `action_scope`, `resource_scope` (can be wildcard patterns like `*`), `requested_action`, `requested_resource` | **DANGEROUS WILDCARD**: patterns like `*` in scope match anything; also canonical provider scopes require exact match (line 811-812 `_is_canonical_provider_scope()`) but **non-provider scopes allow fnmatchcase wildcards** → subtle difference | Overly broad wildcard scopes grant unintended access |
| **4.1** | **Delegation Direction Validation** | [delegation_graph.py:253-272](packages/caracal-server/caracal/core/delegation_graph.py#L253-L272) `validate_delegation_direction()` checks (source_kind, target_kind) against ALLOWED_DELEGATIONS dict | `source_principal_kind`, `target_principal_kind` (from principals) | **NONE DETECTED** - enforced at add_edge time | SERVICE→WORKER, WORKER→HUMAN, etc. escalations allowed → authority bypass |
| **4.2** | **Delegation Edge Scope Amplification Check** | [delegation_graph.py:237-251](packages/caracal-server/caracal/core/delegation_graph.py#L237-L251) `_validate_non_amplifying_target_scope()` ensures target scope ⊆ union of all inbound sources | `source_mandate.resource_scope`, `source_mandate.action_scope`, `target_mandate.resource_scope/action_scope` | Gets all inbound edges via `get_authority_sources()` and checks coverage by union → **tight check** | Delegation scope amplification allowed → delegatee gains more authority than delegator had |
| **4.3** | **Active Mandate Verification in Edges** | [delegation_graph.py:146-155](packages/caracal-server/caracal/core/delegation_graph.py#L146-L155) `_is_mandate_active()` checks not revoked + within valid_from/until window | `mandate.revoked`, `mandate.valid_from`, `mandate.valid_until` | **NONE DETECTED** - tight check, but relies on current time being reliable | Revoked mandates still in DB → edge could re-activate if mandate un-revoked (impossible in current design) |
| **5.1** | **Caveat Chain HMAC Integrity** | [caveat_chain.py:93-125](packages/caracal-server/caracal/core/caveat_chain.py#L93-L125) `verify_caveat_chain()` checks cumulative HMAC linkage (each node includes previous_hmac) | `caveat_chain` (list of dicts with hmac, type, value, index, previous_hmac), `hmac_key` (must be non-empty) | **EMPTY KEY CHECK**: validates key is non-empty (line 168-170), else raises error → fail-closed | Tampered caveat chain undetected → restrictions bypassed |
| **5.2** | **Caveat Index Sequence Validation** | [caveat_chain.py:106-108](packages/caracal-server/caracal/core/caveat_chain.py#L106-L108) checks `node_index == expected_index` (0, 1, 2, ...) | `caveat_chain[i].index` (must be sequential integers) | **NONE DETECTED** - strict index check | Out-of-order caveat entries accepted → evaluation order could be exploited |
| **5.3** | **Caveat Type Restrictions** | [caveat_chain.py:55-70](packages/caracal-server/caracal/core/caveat_chain.py#L55-L70) `parse_caveat()` recognizes ACTION, RESOURCE, EXPIRY, TASK_BINDING; unprefixed treated as RESOURCE | `caveat` string (must start with known prefix or be treated as resource) | **BACKWARD COMPAT**: unprefixed caveats default to RESOURCE type (line 69) → ambiguous | Misinterpreted caveat types → wrong restrictions applied |
| **5.4** | **Caveat Expiry Enforcement** | [caveat_chain.py:187-195](packages/caracal-server/caracal/core/caveat_chain.py#L187-L195) `evaluate_caveat_chain()` checks `min(expiry_constraints) > now` | `expiry_constraints` (datetime list), `current_time` (defaults to now) | Defaults to current UTC time if not provided (line 183) → clock-dependent | Clock drift could expire caveats prematurely or allow expired caveats |
| **6.1** | **Session Token Issuance** | [session_manager.py:300-350](packages/caracal-server/caracal/core/session_manager.py#L300-L350) `issue_session()` creates JWT with ES256/RS256 signature | `principal_id`, `session_kind` (INTERACTIVE/AUTOMATION/TASK), `access_ttl`, `refresh_ttl`, `token_signer` | **SYMMETRIC ALG CHECK**: rejects HS256/HS512 at init time (line 118-121) → fail-closed | HS256-signed tokens treated as valid → HMAC secret could be guessed |
| **6.2** | **Session Token Verification** | [session_manager.py:380-440](packages/caracal-server/caracal/core/session_manager.py#L380-L440) `validate_token()` verifies signature + expiry + deny-list + principal revocation cutoff | `token` (JWT), `verify_key`, `denylist_backend`, algorithm | **OPTIONAL COMPONENTS**: deny-list and revocation cutoff optional (lines 397-410) → can be None → **no runtime deny-list check if not configured** | Revoked tokens still valid if deny-list not enabled |
| **6.3** | **Caveat Chain in Session Tokens** | [session_manager.py:350-365](packages/caracal-server/caracal/core/session_manager.py#L350-L365) `issue_session()` optionally embeds caveat chain in token claims | `caveat_mode` ("jwt" vs "external"), `append_caveats` list | **MODE DEFAULT**: caveat_mode defaults to "jwt" → embedded in token (line 208) → can be read/tampered unless signed properly | Caveat restrictions not enforced at token validation time unless manually verified |
| **7.1** | **Authority Ledger Event Recording** | [authority_ledger.py:185-210](packages/caracal-server/caracal/core/authority_ledger.py#L185-L210) `record_validation()` inserts immutable AuthorityLedgerEvent with decision + denial_reason | `principal_id`, `mandate_id`, `decision` (allowed/denied), `denial_reason`, `requested_action`, `requested_resource` | **OPTIONAL LEDGER WRITER**: ledger writer can be None (lines 135+) → events NOT recorded if not configured | Authority decisions not audited → no proof of enforcement |
| **7.2** | **Ledger Event Atomicity** | [authority_ledger.py:195-205](packages/caracal-server/caracal/core/authority_ledger.py#L195-L205) inserts to DB within same transaction scope | DB session, atomic flush/commit | **NO EXPLICIT TRANSACTION WRAPPER**: relies on caller's session management → event could be lost if session rolled back after decision | Authority decision made but ledger not persisted → audit gap |
| **7.3** | **Merkle Tree Integrity** | [merkle/tree.py:~180-220](packages/caracal-server/caracal/merkle/tree.py#L180-L220) (not fully read, but architecture shows) signs batch of events with ECDSA → creates tamper-evident chain | `ledger_events[]`, `signing_key` (issuer private key), `batch_size` | **BATCHER OPTIONAL**: merkle batcher can be disabled → events appended without merkle signatures | Ledger events could be modified in DB without detection |
| **8.1** | **Principal Revocation Cascade (Leaves-First)** | [revocation.py:115-160](packages/caracal-server/caracal/core/revocation.py#L115-L160) `revoke_principal()` computes leaves-first topological order → revokes mandates & edges sequentially | `principal_id`, `revocation_reason`, `session_token_jtis`, `session_expires_at` | **ASYNC CASCADE THRESHOLD**: large graphs (>250 nodes) trigger async job dispatch (line 131-140) → synchronous path only revokes leaf node → **root revocation incomplete if async fails** | Parent principals remain active after children revoked → authority not fully cascaded |
| **8.2** | **Session Deny-List Recording** | [revocation.py:164-177](packages/caracal-server/caracal/core/revocation.py#L164-L177) `_denylist_sessions()` adds token JTIs to Redis deny-list with TTL | `session_token_jtis[]`, `session_expires_at`, `denylist_backend` | **OPTIONAL DENY-LIST**: deny-list backend can be None (line 163) → tokens not revoked if not configured | Revoked principals' sessions still valid |
| **8.3** | **Principal Revocation Event Publishing** | [revocation.py:189-210](packages/caracal-server/caracal/core/revocation.py#L189-L210) publishes RevocationEventPublisher notifications | `principal_id`, `revoked_principal_ids`, `revoked_mandate_ids`, `revoked_edge_ids` | **OPTIONAL PUBLISHER**: revocation_event_publisher can be None (line 179) → events not published → downstream systems not notified | Gateway, caches, other services miss revocation signals |
| **9.1** | **Scoped Request Enforcement (enforce_scoped_requests flag)** | [broker.py:1097-1115](packages/caracal-server/caracal/deployment/broker.py#L1097-L1115) `_validate_request_scope()` checks canonical provider scope if `config.enforce_scoped_requests=True` OR if request has resource/action | `enforce_scoped_requests` bool (defaults to False), `request.resource`, `request.action`, provider definition | **DEFAULT FAIL-OPEN**: `enforce_scoped_requests` defaults to False (line 124 in broker.py) → **non-scoped requests allowed by default** → line 1097 "if not request.resource and not request.action and not config.enforce_scoped_requests: return" → **skips validation entirely if flag off** | Unscoped provider requests bypass scope contract checks → access to any action |
| **9.2** | **Provider Definition Lookup** | [broker.py:1100-1110](packages/caracal-server/caracal/deployment/broker.py#L1100-L1110) calls `_get_definition_for_provider()` to resolve provider definition | `provider` name, `config.enforce_scoped_requests`, provider registry | Definition lookup can fail (lines 1120+) → **raises ProviderConfigurationError** → request fails (fail-closed) | N/A |
| **9.3** | **HTTP Method & Path Validation** | [broker.py:1113-1130](packages/caracal-server/caracal/deployment/broker.py#L1113-L1130) (inferred) validates that request HTTP method matches provider action definition method + path matches prefix | `request.method`, `request.endpoint`, `definition.actions[action_id].method`, `definition.actions[action_id].path_prefix` | If validation fails, raises ProviderAuthorizationError (line 1130) → fail-closed | Provider action executed with wrong HTTP method/path → API contract violation |
| **10.1** | **Principal Lifecycle Attestation Gating** | [identity.py:165-167](packages/caracal-server/caracal/core/identity.py#L165-L167) for ORCHESTRATOR/WORKER: forces PENDING_ATTESTATION if not ATTESTED, else ACTIVE | `principal_kind`, `attestation_status`, `lifecycle_status` (input) | **BYPASSABLE VIA DIRECT FIELD**: `lifecycle_status` parameter can override the gating (e.g., register with lifecycle_status=ACTIVE + attestation_status=UNATTESTED) → **gate only prevents auto-transition, doesn't block direct setting** → line 165 checks `if (principal_kind in non_reactivating_kinds and attestation_status != ATTESTED): lifecycle_status = PENDING_ATTESTATION` → **but only if UNATTESTED, doesn't validate against ATTESTED false-positive** | Attestation requirement bypassed → unattest ORCHESTRATOR/WORKER agents register as ACTIVE |
| **10.2** | **Lifecycle State Transitions** | [lifecycle.py:~50-100](packages/caracal-server/caracal/core/lifecycle.py) `PrincipalLifecycleStateMachine.assert_transition_allowed()` (implementation not fully read) enforces state machine rules | `principal_kind`, `from_status`, `to_status`, `attestation_status` | **GATED BY ATTESTATION**: ORCHESTRATOR/WORKER cannot PENDING_ATTESTATION→ACTIVE without ATTESTED status | Strict state machine but could have logic bugs in transition matrix |
| **11.1** | **Edition Detection (OPENSOURCE vs ENTERPRISE)** | [edition.py:75-155](packages/caracal-server/caracal/deployment/edition.py#L75-L155) `EditionManager.get_edition()` checks (1) CCL_GATEWAY_ENABLED env var, (2) persisted config, (3) default OPENSOURCE | `CCL_GATEWAY_ENABLED` env var (truthy/falsy), `config.toml` file, DEFAULT_EDITION | **ENV VAR BYPASS**: CCL_GATEWAY_ENABLED can be set to "1"/"true"/"yes"/"on" → Edition.ENTERPRISE (line 141) → **single env var changes deployment mode** → no authentication required to toggle | ENTERPRISE code paths (gateway routing) activated in OSS deployment |
| **11.2** | **Edition Caching** | [edition.py:91-104](packages/caracal-server/caracal/deployment/edition.py#L91-L104) result cached in memory (self._cached_edition) → avoids repeated file I/O | `_cached_edition`, `_cache_timestamp` | **NO CACHE EXPIRY**: cached value never refreshed (only set once at init) → env var changes after startup ignored | Edition flipped at runtime without effect |
| **11.3** | **Enterprise Config Loading** | [edition.py:113-139](packages/caracal-server/caracal/deployment/edition.py#L113-L139) `_gateway_url_from_config()` loads from enterprise_runtime or config.toml | `enterprise_runtime` (DB), `config.toml` file | **EXCEPTION SWALLOWING**: line 127 `except Exception: logger.debug(...)` swallows all errors → silently falls back to config file | Gateway URL load failure goes unnoticed |
| **12.1** | **Enterprise Admin Authentication** | [gateway.py:81-85](caracalEnterprise/services/api/src/caracal_api/routes/gateway.py#L81-L85) `_require_admin()` checks `current_user.get("is_admin")` | `current_user` dict (from `get_current_user()` dependency), `is_admin` claim | **CLAIM-BASED ONLY**: no role re-validation against DB → relies on token claims → if token issuer compromised or claim forged, check bypassed | Unauthorized users escalate via forged admin claim |
| **12.2** | **Workspace Admin Enforcement** | [team.py:428-437](caracalEnterprise/services/api/src/caracal_api/routes/team.py#L428-L437) "Prevent removing the last admin" checks `admin_count <= 1` | `user.role` (from DB), `admin_count` (result of `count_active_admins()`) | **RACE CONDITION**: count is not atomic with role update → two concurrent removals could both see count=2 → both proceed → zero admins result | Last admin removed → workspace orphaned |
| **12.3** | **Enterprise API Workspace Scoping** | [caracal_core.py:47-60](caracalEnterprise/services/api/src/caracal_api/integrations/caracal_core.py#L47-L60) `EnterpriseCaracalClient.__init__()` hardcodes `workspace_id` → all queries filtered by `Principal.owner == self.workspace_id` | `workspace_id` (from client initialization), database queries | **HARDCODED AT INIT**: workspace scope cannot change per-request → tenant must be pre-validated before client creation (responsibility pushed to caller) | Wrong workspace passed to client → data leaked across tenants |
| **12.4** | **Enterprise Mandate Issuance Tenant Check** | [caracal_core.py:243-290](caracalEnterprise/services/api/src/caracal_api/integrations/caracal_core.py#L243-L290) `issue_mandate()` validates issuer + subject `owner == self.workspace_id` before calling Core's MandateManager | `issuer_id`, `subject_id`, `workspace_id` (self.workspace_id) | **TWO-STEP VALIDATION**: (1) issuer must belong to workspace, (2) subject must belong to workspace → both required (lines 255-265 + 268-278) → tight check | Different-workspace principal can receive mandate → cross-tenant authority leak |

---

## CRITICAL SECURITY FLAGS

### ⛔ FAIL-OPEN VULNERABILITIES (Default Allow)

1. **enforce_issuer_policy=False** [mandate.py:296] — Entire policy enforcement can be skipped by setting flag to False
2. **enforce_scoped_requests=False** [broker.py:1097] — Scope validation completely skipped if flag off (default)
3. **Mandate cache optional** [authority.py:136] — If no cache configured, falls back to DB (OK, but no caching benefit)
4. **Rate limiter optional** [mandate.py:318] — Issuance rate limiting skipped if not configured
5. **Delegation graph optional** [authority.py:705] — Path validation skipped if DelegationGraph not provided
6. **Caveat chain optional** [authority.py:761] — Caveat restrictions skipped if chain is None/empty
7. **Ledger writer optional** [authority_ledger.py:135] — Authority decisions not audited if ledger writer not configured
8. **Deny-list optional** [session_manager.py:397] — Revoked tokens still valid if deny-list backend not configured
9. **Revocation publisher optional** [revocation.py:179] — Downstream systems not notified of principal revocation
10. **Async cascade threshold** [revocation.py:131] — Large principal graphs revoke only root node synchronously; descendants handled asynchronously → root can still act until async job completes

### ⚠️ ATTESTATION BYPASS (lifecycle.py + identity.py)

- **Line 165-167 (identity.py)**: ORCHESTRATOR/WORKER attestation gating only auto-sets PENDING_ATTESTATION if `attestation_status != ATTESTED`
- **EXPLOIT**: Caller can pass `attestation_status="unattested"` + `lifecycle_status="active"` directly → lifecycle gating checks only IF attestation status is not ATTESTED, but doesn't validate that ATTESTED is actually true
- **RESULT**: Unattested ORCHESTRATOR/WORKER principals can register as ACTIVE

### ⚠️ EDITION BYPASS (edition.py)

- **Line 141-143**: `CCL_GATEWAY_ENABLED` env var is **only check** for edition mode
- **NO AUTHENTICATION**: Any process with env var access can switch from OPENSOURCE to ENTERPRISE
- **NO CACHE EXPIRY**: Edition cached at startup; changes ignored at runtime
- **RESULT**: ENTERPRISE features (gateway routing, advanced auth) activated in OSS deployment without re-authentication

### ⚠️ BROAD EXCEPT: PASS (Silent Error Swallowing)

| File | Line | Issue |
|---|---|---|
| [revocation.py:178](packages/caracal-server/caracal/core/revocation.py#L178) | `except Exception:` in `_invalidate_mandate_cache_for_principal()` | No action on cache invalidation failure → stale cache remains |
| [authority.py:208](packages/caracal-server/caracal/core/authority.py#L208) | `except Exception:` in signature verification loop | Continues to next mandate if verification fails → errors silently ignored |
| [edition.py:127](packages/caracal-server/caracal/deployment/edition.py#L127) | `except Exception: logger.debug(...)` | Enterprise config load failure goes unnoticed |

### ⚠️ MISSING CHECKS / INCOMPLETE VALIDATION

| Decision Point | Missing Check | Impact |
|---|---|---|
| **Issuer Authority (Stage 1)** [authority.py:559] | No attestation requirement for issuers | Suspended (but not revoked) issuers can still issue mandates |
| **Subject Binding (Stage 4)** [authority.py:736] | Assumes mandate.subject_id == caller_principal_id; no override check | Mandate can be used by any principal if subject binding not enforced at request layer |
| **Rate Limiter Integration** [mandate.py:318-327] | Rate limit check optional; errors recorded but NOT re-raised if rate_limiter exists but fails | Rate limit failure logged but mandate issued anyway |
| **Ledger Transaction Atomicity** [authority_ledger.py:195-205] | No explicit transaction wrapper | Authority decision made, but ledger event lost if session rolled back |
| **Admin Token Claims** [gateway.py:81-85] | No re-validation against DB; relies on token issuer | Forged admin claims bypass DB checks |
| **Last Admin Guard (Race)** [team.py:428-437] | Count not atomic with removal | Two concurrent removals could both see count≥2 |

### ⚠️ ENVIRONMENT VARIABLE BYPASSES

| Env Var | Purpose | Bypass Risk |
|---|---|---|
| `CCL_GATEWAY_ENABLED` | Edition selection (OPENSOURCE/ENTERPRISE) | Single env var switches deployment mode without re-auth |
| `CCL_ALLOW_INTERNAL_PROVIDER_URLS` | Dev mode provider URL validation | Allows localhost/private IP providers in dev mode |
| `CCL_ENV_MODE` | Dev/test/prod detection | Combined with above allows metadata service access in dev |
| `CCL_AIS_ATTESTATION_NONCE` | Attestation nonce (enterprise gateway) | Can be set to bypass nonce uniqueness (if checking is not implemented) |

### ⚠️ HARD-CUT ENTERPRISE-SPECIFIC ISSUES

1. **Workspace Isolation at Init** [caracal_core.py:47]: `workspace_id` hardcoded at EnterpriseCaracalClient init — scope cannot change per-request; tenant pre-validation must occur before client creation (caller responsibility)

2. **Admin Claim Validation** [gateway.py:81]: Only checks `current_user.get("is_admin")` — no DB re-fetch; if JWT issuer is compromised, forged admin claims work

3. **Last Admin Race Condition** [team.py:428]: Count + removal not atomic; two concurrent requests could both see safe count then both proceed

---

## SUMMARY TABLE: Decision Matrix

| Coverage Area | Status | Risk Level | Notes |
|---|---|---|---|
| **Principal Registration** | ✅ Secure | LOW | Uniqueness enforced; attestation bypass via direct field setting (MEDIUM) |
| **Mandate Issuance** | ⚠️ Policy Optional | HIGH | `enforce_issuer_policy=False` bypasses all checks |
| **Authority Validation (6 stages)** | ⚠️ Components Optional | HIGH | Path validation, caveat chain, ledger all optional |
| **Delegation Graph** | ✅ Secure | LOW | Direction rules enforced; scope amplification checked |
| **Caveat Chain** | ✅ Secure | LOW | HMAC integrity verified; expiry checks time-dependent |
| **Session Tokens** | ⚠️ Deny-list Optional | MEDIUM | Revocation checks skipped if deny-list not configured |
| **Authority Ledger** | ⚠️ Ledger Optional | MEDIUM | Events not recorded if writer not configured |
| **Revocation Cascades** | ⚠️ Async Incomplete | MEDIUM | Large graphs use async cascade → root still active until async completes |
| **Scope Enforcement** | ⚠️ Scoped Requests Optional | HIGH | Default allow (enforce_scoped_requests=False) bypasses contract checks |
| **Lifecycle State Transitions** | ⚠️ Attestation Bypass | MEDIUM | Attestation gating can be bypassed via direct lifecycle_status setting |
| **Edition Detection** | ⚠️ Env Var Only | HIGH | CCL_GATEWAY_ENABLED env var sole mode selector; no re-auth |
| **Enterprise Admin Auth** | ⚠️ Claim-Based | MEDIUM | No DB re-validation; forged JWT claims work |